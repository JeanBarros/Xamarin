<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BoasVindas"
             x:Class="BoasVindas.MainPage">

    <StackLayout Spacing="20" VerticalOptions="Center" HorizontalOptions="Center">

        <Label Text="Xamarin Forms - MVVM" TextColor="Navy" FontSize="20"></Label>

        <Entry Text="{Binding Nome}" Placeholder="Informe seu nome" FontSize="20"></Entry>

        <Button Text="Boas Vindas" TextColor="Bisque" BackgroundColor="Teal" 
                FontSize="20" 
                Command="{Binding BoasVindasCommand}"></Button>

        <Label Text="{Binding Mensagem}" 
               FontSize="30" TextColor="Red" 
               FontAttributes="Bold"></Label>
    
    </StackLayout>

</ContentPage>
